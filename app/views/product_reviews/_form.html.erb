<%= form_with(model: product_review) do |form| %>
  <% if product_review.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_review.errors.count, "error") %> prohibited this product_review from being saved:</h2>

      <ul>
        <% product_review.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :rating, style: "display: block" %>
    <%= form.number_field :rating %>
  </div>

  <div>
    <%= form.label :review_text, style: "display: block" %>
    <%= form.text_area :review_text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
