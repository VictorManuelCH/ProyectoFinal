<%= form_with(model: payment_state) do |form| %>
  <% if payment_state.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(payment_state.errors.count, "error") %> prohibited this payment_state from being saved:</h2>

      <ul>
        <% payment_state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
