<%= form_with(model: order_state) do |form| %>
  <% if order_state.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(order_state.errors.count, "error") %> prohibited this order_state from being saved:</h2>

      <ul>
        <% order_state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :order_id, style: "display: block" %>
    <%= form.text_field :order_id %>
  </div>

  <div>
    <%= form.label :state_id, style: "display: block" %>
    <%= form.text_field :state_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
